    import XCTest
    @testable import AllApplesSplitView

    final class AllApplesSplitViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AllApplesSplitView().text, "Hello, World!")
        }
    }
